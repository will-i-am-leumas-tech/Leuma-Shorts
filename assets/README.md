# Demo Assets

This directory is intentionally generated at runtime.

Run:

```bash
node scripts/bootstrap-demo-assets.js
```

That script creates small synthetic gameplay/media assets so the SDK can render example jobs without committing large binaries.
